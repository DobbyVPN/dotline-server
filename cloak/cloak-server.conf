{
  "ProxyBook": {
    "shadowsocks": [
      "tcp",
      "shadowbox:${OUTLINE_KEYS_PORT}"
    ]
      },
  "BindAddr": [
    "0.0.0.0:${CLOAK_SERVER_PORT}"
    ],
  "BypassUID": [
    "${CLOAK_USER_UID}"
  ],
  "AdminUID": "${CLOAK_ADMIN_UID}",
  "RedirAddr": "${DOMAIN_NAME}",
  "PrivateKey": "${CLOAK_PRIVATE_KEY}",
  "StreamTimeout": 300
}

